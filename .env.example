# 本机客户端配置：只告诉 trigger/fetch 脚本如何 SSH 进评测服务器。
# 这个文件不需要、也不该放 DeepSeek Key（Key 留在服务器侧）。
# 复制为 .env 后按需修改。

# 评测服务器地址（支持密码或私钥二选一）
TB_BENCH_HOST=your.server.ip
TB_BENCH_USER=root
TB_BENCH_PASSWORD=
TB_BENCH_KEY=~/.ssh/id_ed25519

# 服务器上的工作目录
TB_BENCH_WORKDIR=/root/psi-agent-benchmark
